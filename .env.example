# Copy to `.env` and fill in your key (the real .env is gitignored).
# Used by the /api/chat serverless function to talk to Mistral AI.
# In production, set the same variable in your Vercel project settings.
MISTRAL_AI_KEY=
